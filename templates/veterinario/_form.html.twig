<div class="shadow-sm border rounded bg-white">
    <div class="p-4">
        {{ form_start(form) }}

        <div class="mb-3">
            {{ form_label(form.nome, 'Nome do Veterinário *', {'label_attr': {'class': 'form-label'}}) }}
            {{ form_widget(form.nome, {'attr': {'class': 'form-control'}}) }}
        </div>

        <div class="mb-3">
            {{ form_label(form.crmv, 'CRMV *', {'label_attr': {'class': 'form-label'}}) }}
            {{ form_widget(form.crmv, {'attr': {'class': 'form-control'}}) }}
        </div>

        <div class="d-flex justify-content-end gap-2">
            <button class="btn btn-primary">{{ button_label|default('Salvar') }}</button>
            <a class="btn btn-secondary" href="{{ path('app_veterinario_index') }}">Voltar</a>
        </div>

        {{ form_end(form) }}
    </div>
</div>
