<form method="post" action="{{ path('app_gado_delete', {'id': gado.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ gado.id) }}">
    <button class="btn btn-danger">Delete</button>
</form>
